{
  "tasks": {
    "develop": "deno run --watch --check --unstable --allow-read --allow-write --allow-net",
    "test": "deno test --unstable --check --import-map test/tools/import_map.json --allow-read --allow-write --allow-net test/*.test.ts",
    "test:watch": "deno test --unstable --watch --check --trace-ops --import-map test/tools/import_map.json --allow-read --allow-write --allow-net",
    "lint": "deno lint",
  }
}
