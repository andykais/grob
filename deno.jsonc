{
  "tasks": {
    "develop": "deno run --watch --check --unstable --allow-read --allow-write --allow-net",
    "test:watch": "deno test --watch --check --unstable --allow-read --allow-write --allow-net test/grob.test.ts",
    "lint": "deno lint",
  }
}
